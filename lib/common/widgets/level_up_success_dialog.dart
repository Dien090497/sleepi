import 'package:flutter/material.dart';

class LevelUpSuccessDialog extends StatelessWidget {
  const LevelUpSuccessDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
