import 'package:flutter/material.dart';

class SendToSpendingScreen extends StatelessWidget {
  const SendToSpendingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
